# تطوير محلي — بروكسي Vite على /api (Same-Origin مثل Pages)
# اترك VITE_API_URL فارغاً؛ المسارات في api-client تبدأ بـ /api/...

# هدف البروكسي: Worker محلي (wrangler dev) أو منشور
# محلي (موصى به عند تشغيل wrangler dev --port 8787):
# VITE_API_PROXY_TARGET=http://127.0.0.1:8787
VITE_API_PROXY_TARGET=https://winter-term-cb93.a-samani092.workers.dev

# لا تضبط عنواناً مطلقاً هنا — يُكسر بروكسي Vite
VITE_API_URL=
